#!/bin/sh
# Template only. Future implementation should stop lifecycle-owned runtime state.
set -eu

printf '%s\n' 'LXC on DSM preupgrade template: no action implemented.'
printf '%s\n' 'Future preupgrade should stop managed containers before payload changes.'
